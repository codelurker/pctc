"""Library to do the heavy lifting for PCTC.

Modules:
    backend: contains the Twitter interface
    config:  reads and manages config files
    ui:      controls the UI

Constants:
    version: current version of PCTC as a tuple
"""

version = (0, 0, 1)
